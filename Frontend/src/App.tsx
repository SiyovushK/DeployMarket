/**
 * App is mounted inside RouterProvider via the router tree.
 * Global side-effect hooks (useFavoritesSync, useProfileSync)
 * are mounted once in GlobalProvider inside router/index.tsx.
 * This file is kept as a placeholder — add app-level providers here if needed.
 */
export default function App() {
  return null
}
